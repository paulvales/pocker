After code changes, run `npm test` at minimum. If the change affects realtime room behavior or UI interaction timing, also consider `npm run stress:votes` and a manual multi-user browser check. Keep Docker/Compose compatibility intact and avoid breaking existing room-link and Socket.IO client/server contracts. Summarize any untested browser-only behavior explicitly in the final response.