Project uses JavaScript/CommonJS with manual formatting and no enforced linter/formatter config. Follow existing naming: lowercase kebab-case files, camelCase functions/locals, UPPER_SNAKE_CASE constants, create* factory names. Error handling uses string error codes like FORBIDDEN / ROOM_NOT_FOUND and socket ack envelopes { ok: boolean, error?: string }. Logging uses shared pino logger with structured objects. Keep comments short and only for non-obvious operational decisions. Preserve existing room-link UX and Socket.IO event contracts.